import { BaseCharacter, Character, CharacterConstructor } from "rpg-ts";

const Archer = new Character({
    name: "Archer",
    stats: {
        hp: 100,
    },
    mana: 100,
});

Archer.

export default Archer;