import { BASE_STATS, CLASS_STATS } from '../jsons/stats'
import { rand } from './utils'

const createHero = () => {
    let id_class = rand(0, CLASS_STATS.length -1)
}


const getCharacterStats = () => {
    let basic_stats = BASE_STATS


}

export {
    getCharacterStats
}